# Contributing

Contributions should improve reproducibility, domain-independent interfaces, evidence methods, metrics, documentation or defensive benchmark coverage.

Requirements:
- keep attack modules simulation-only and in-memory;
- add tests for behavioral changes;
- document units and parameter provenance;
- distinguish synthetic, simulated and measured data;
- do not claim operational validation without evidence;
- preserve benchmark comparability or version the protocol when changing it.
